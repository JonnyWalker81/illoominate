---
phase: 01-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/components/Hero.astro
  - src/components/Features.astro
  - src/components/WaitlistForm.astro
autonomous: true

must_haves:
  truths:
    - "Visitor sees clear value proposition about native SDKs and startup-friendly pricing"
    - "Landing page has hero section with headline, tagline, and CTA"
    - "Landing page has features section with 3-4 feature cards"
    - "Landing page has waitlist signup form with email field"
    - "Page has dark mode aesthetic matching Linear/Vercel"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Hero section with headline, tagline, CTA button"
      min_lines: 50
    - path: "src/components/Features.astro"
      provides: "Feature cards grid section"
      min_lines: 80
    - path: "src/components/WaitlistForm.astro"
      provides: "Waitlist signup form with success state"
      min_lines: 60
    - path: "src/pages/index.astro"
      provides: "Main landing page composing all sections"
      contains: "Hero"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/Hero.astro"
      via: "component import"
      pattern: "import Hero"
    - from: "src/pages/index.astro"
      to: "src/components/WaitlistForm.astro"
      via: "component import"
      pattern: "import WaitlistForm"
---

<objective>
Create the landing page frontend with Hero, Features, and Waitlist form sections using the /frontend-design skill for distinctive, production-grade UI.

Purpose: Deliver the visual experience that communicates Illoominate's value proposition to visitors.
Output: Complete landing page with dark mode Linear/Vercel aesthetic, ready for API integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-page/01-CONTEXT.md
@.planning/phases/01-landing-page/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero section with /frontend-design skill</name>
  <files>src/components/Hero.astro</files>
  <action>
    Invoke the /frontend-design skill to create the hero section.

    **Context to provide to /frontend-design:**
    ```
    Purpose: Landing page hero for Illoominate, a native-first user feedback platform
    Target: Indie developers and startups
    Aesthetic: Linear meets Vercel - clean, dark, developer-focused but approachable

    Visual Identity (from CONTEXT.md):
    - Dark mode default (dark backgrounds, light text)
    - Cool color palette (blue/purple) for primary accents
    - Geometric/modern typography - Geist font
    - Minimal/abstract backgrounds
    - Linear/Vercel design inspiration

    Tone: Friendly professional - warm but competent, approachable startup vibe
    Differentiation: Native-first, indie-friendly alternative to Canny
    ```

    **Requirements for Hero.astro:**
    - Headline communicating native-first feedback for indie devs
    - Tagline about startup-friendly pricing (no specific amounts)
    - Single CTA button that scrolls to waitlist form (#waitlist)
    - NO product visuals in hero (per CONTEXT.md)
    - Staggered fade-in animation on page load
    - Subtle gradient background with noise texture overlay
    - Use CSS custom properties from global.css (--color-primary-*, etc.)

    **Implementation details:**
    - Use Geist font (already loaded in Layout)
    - Headline: 700 weight, large size (text-5xl md:text-7xl)
    - Tagline: 400 weight, secondary text color
    - CTA: Primary indigo button with hover glow effect
    - Add staggered animation classes (hero-headline, hero-tagline, hero-cta)
    - Background: radial gradient with mesh effect + noise overlay

    **Copy direction:**
    - Headline: Focus on native SDK differentiation ("Native feedback, not webviews")
    - Tagline: Mention indie/startup focus, pricing that doesn't punish success
    - CTA: "Join the Waitlist"

    **Avoid:**
    - Inter or Roboto fonts
    - Purple gradients on white backgrounds
    - Generic SaaS aesthetic
    - Heavy shadows
  </action>
  <verify>
    - Component renders without errors
    - Animations play on page load (fade in from bottom)
    - Hover effects work on CTA button (translate + glow)
    - Background has subtle gradient (not solid black)
    - CTA links to #waitlist section
  </verify>
  <done>
    Hero section complete with distinctive Linear/Vercel aesthetic, proper animations, and clear value proposition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Features section with /frontend-design skill</name>
  <files>src/components/Features.astro</files>
  <action>
    Invoke the /frontend-design skill to create the features section.

    **Context to provide to /frontend-design:**
    ```
    Purpose: Feature showcase section for Illoominate landing page
    Aesthetic: Linear meets Vercel - clean, dark, minimal, developer-focused

    Visual Identity:
    - Dark mode (dark backgrounds, light text)
    - Cool blue/purple accent colors
    - Geist font family
    - Subtle hover effects
    ```

    **Requirements for Features.astro:**
    - 3-4 feature cards in a responsive grid (1 col mobile, 3-4 cols desktop)
    - Each card: icon + title + brief description
    - Icons should be minimal line-style (use SVG, not filled)
    - Subtle hover effect on cards (slight scale or border glow)
    - Mesh gradient background (different from hero)

    **Features to highlight (from CONTEXT.md):**
    1. **Native SDKs** - iOS and Web SDKs that feel like part of your app, not an embedded webview
    2. **Dashboard & Organization** - Boards, statuses, and voting to keep feedback organized
    3. **Public Roadmap** - Share what you're building, let users vote on priorities
    4. **Startup-Friendly Pricing** - Pricing that grows with you, not against you

    **Implementation details:**
    - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6`
    - Cards: bg-bg-secondary with subtle border, rounded-lg
    - Icons: 32x32 or 40x40, stroke style, primary color
    - Hover: `transform hover:scale-[1.02]` or border color change
    - Background: mesh gradient with 2-3 radial gradients at low opacity

    **Avoid:**
    - Heavy card shadows
    - Gradient cards (keep cards solid)
    - Filled/solid icons
    - Too much visual noise
  </action>
  <verify>
    - Grid displays correctly at mobile, tablet, desktop breakpoints
    - All 4 features render with icons, titles, descriptions
    - Hover effects work on cards
    - Background mesh gradient visible (subtle color pools)
  </verify>
  <done>
    Features section complete with 4 feature cards, responsive grid, and subtle hover effects.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create WaitlistForm section and compose landing page</name>
  <files>
    src/components/WaitlistForm.astro
    src/pages/index.astro
  </files>
  <action>
    **Part A: Create WaitlistForm.astro**

    Invoke the /frontend-design skill for the form styling.

    **Context to provide:**
    ```
    Purpose: Waitlist signup form for Illoominate
    Aesthetic: Linear/Vercel dark minimal
    State: Form has two states - input form and success display
    ```

    **Requirements for WaitlistForm.astro:**
    - Section with id="waitlist" for anchor link from hero CTA
    - Form with:
      - Email input (required)
      - Name input (optional)
      - "How did you hear about us?" select (optional)
      - Submit button (full width, primary style)
    - Success state (hidden by default):
      - Large position number (#47)
      - "You're on the list!" message
      - Referral code display with copy button
      - Link to optional quiz
    - Loading state on submit button
    - Client-side form handling with fetch to /api/waitlist
    - Error state display

    **Implementation details:**
    - Inputs: bg-bg-secondary, border-gray-700, rounded-lg, focus:border-primary-500
    - Submit button: matches hero CTA style (indigo, hover glow)
    - Success state: fade in animation, large bold position number
    - Referral code: monospace font, copy-to-clipboard functionality
    - Max-width container (max-w-md) centered

    **Part B: Compose src/pages/index.astro**

    Import and arrange all sections:
    1. Hero
    2. Features
    3. WaitlistForm

    Add:
    - Smooth scroll behavior for anchor links
    - Page metadata (title, description)
    - Optional: simple footer with copyright

    **Note:** Form submission will POST to /api/waitlist but API doesn't exist yet.
    The form should handle errors gracefully (show error message, don't crash).
  </action>
  <verify>
    - Form renders with all fields
    - Required email validation works (HTML5 validation)
    - Submit button shows loading state when clicked
    - Clicking hero CTA smoothly scrolls to waitlist section
    - Success state is hidden by default (form visible)
    - Mobile responsive (form fields stack, readable on small screens)
  </verify>
  <done>
    WaitlistForm complete with form and success states, landing page composed with all sections.
  </done>
</task>

</tasks>

<verification>
Landing page frontend is complete when:
1. All three sections render (Hero, Features, WaitlistForm)
2. Page has consistent dark mode aesthetic
3. Animations work (hero fade-in, button hovers)
4. Form fields are styled consistently with dark theme
5. Page is responsive at mobile, tablet, desktop
6. Geist font is applied to all text
7. Smooth scroll works from hero CTA to waitlist
</verification>

<success_criteria>
- Landing page communicates native SDKs + startup-friendly pricing value prop
- Visual aesthetic matches Linear/Vercel inspiration (dark, minimal, blue/purple accents)
- All sections use Geist font and consistent color palette
- Form is ready for API integration (sends POST to /api/waitlist)
- Page is responsive and accessible (contrast, focus states)
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page/01-02-SUMMARY.md`
</output>
