---
phase: 01-landing-page
plan: 05
type: execute
wave: 2
depends_on: ["01-04"]
gap_closure: true
files_modified:
  - src/db/schema.ts
  - src/lib/validation.ts
  - src/components/QuizModal.astro
  - src/pages/api/quiz.ts
autonomous: true

must_haves:
  truths:
    - "Quiz shows one question per screen with progress bar"
    - "5 questions: role, platform, team size, PMF, pain points"
    - "Visual card choices with icons for selection"
    - "Personalized result screen with type and position boost"
    - "Quiz completion boosts waitlist position by 5 spots"
  artifacts:
    - path: "src/components/QuizModal.astro"
      provides: "Step-by-step quiz wizard with 5 questions"
      min_lines: 400
    - path: "src/pages/api/quiz.ts"
      provides: "Quiz API with position boost logic"
      exports: ["POST"]
    - path: "src/db/schema.ts"
      contains: "disappointment_level"
    - path: "src/lib/validation.ts"
      contains: "disappointmentLevel"
---

<objective>
Redesign the waitlist quiz from a single-page form to an engaging one-question-per-screen wizard with progress tracking, visual card choices, and personalized results.

Purpose: Increase quiz completion rate through proven UX patterns (single question per screen, progress bar, visual choices).
Output: Complete quiz redesign with 5-step wizard, PMF question, position boost, and personalized result screen.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-page/01-CONTEXT.md
@.planning/phases/01-landing-page/01-04-SUMMARY.md
@src/components/QuizModal.astro
@src/pages/api/quiz.ts
@src/db/schema.ts
@src/lib/validation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update database schema and validation</name>
  <files>
    src/db/schema.ts
    src/lib/validation.ts
  </files>
  <action>
    **Part A: Update src/db/schema.ts**
    
    Add new fields to quiz_responses table:
    - `role` (text): developer, founder, pm, designer, other
    - `disappointment_level` (text): very, somewhat, not
    - `quiz_completed` (integer): 1 if completed, 0 if skipped
    
    **Part B: Update src/lib/validation.ts**
    
    Update quizSchema to include:
    - `role` enum: developer, founder, pm, designer, other
    - `disappointmentLevel` enum: very, somewhat, not
    - All fields optional except waitlistId
    
    **Part C: Generate new migration**
    
    Run: `npx drizzle-kit generate`
  </action>
  <verify>
    - Schema includes role and disappointment_level columns
    - Validation schema accepts new fields
    - Migration file generated
  </verify>
  <done>
    Database schema updated with role and disappointmentLevel fields, migration generated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign QuizModal as step-by-step wizard</name>
  <files>
    src/components/QuizModal.astro
  </files>
  <action>
    Transform QuizModal from single-page form to 5-step wizard:
    
    **Step 1 - Role (Hook question):**
    - "What's your primary role?"
    - Options: Developer, Founder, PM, Designer, Other
    - Large visual cards with relevant icons
    
    **Step 2 - Platform:**
    - "What platforms do you build for?"
    - Options: iOS, Android, Web, Multiple
    - Visual cards with platform icons
    
    **Step 3 - Team Size:**
    - "How big is your team?"
    - Options: Solo, 2-5, 6-20, 20+
    - Visual cards with team icons
    
    **Step 4 - PMF Question:**
    - "How disappointed would you be if you couldn't use native-feeling feedback tools?"
    - Options: Very disappointed, Somewhat disappointed, Not disappointed
    - This is the Superhuman PMF metric
    
    **Step 5 - Pain Points (optional):**
    - "What's your biggest feedback challenge?"
    - Textarea for open response
    - Skip button prominently available
    
    **UI Requirements:**
    - Progress bar at top (Step X of 5 + visual progress)
    - Back button to go to previous question (hidden on step 1)
    - Smooth slide/fade animations between steps
    - Large clickable cards (not radio buttons)
    - Cards highlight on selection with indigo accent
    - Auto-advance to next question on selection
    - Skip option only on step 5 (pain points)
    
    **Result Screen:**
    - Show after completing all questions
    - Display personalized "type" based on answers
    - Show position boost: "Your answers moved you up 5 spots!"
    - Celebratory visual (confetti or success animation)
  </action>
  <verify>
    - Modal shows one question at a time
    - Progress bar updates correctly
    - Back button navigates to previous question
    - Cards are clickable and highlight on selection
    - Animations are smooth between steps
    - Result screen shows personalized type
  </verify>
  <done>
    QuizModal redesigned as 5-step wizard with progress bar, visual cards, and personalized results.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update quiz API with position boost logic</name>
  <files>
    src/pages/api/quiz.ts
  </files>
  <action>
    Update the quiz API to:
    
    1. Accept new fields (role, disappointmentLevel)
    2. Save all quiz response data
    3. Implement position boost logic:
       - Calculate boost based on quiz completion (5 spots)
       - Return new position after boost
       - Return personalized "type" based on answers
    
    **Position boost implementation:**
    - When quiz is completed, decrease referralCount by -5 equivalent
    - Actually: Add a `quizBoost` column to waitlist table OR
    - Simpler: Just return the boosted position (position - 5) in response
    
    **Personalized type algorithm:**
    Based on role + platform combination:
    - iOS + Developer/Founder = "iOS Pioneer"
    - Android + Developer/Founder = "Android Innovator"
    - Web + Developer/Founder = "Web Architect"
    - Multiple + any = "Multi-Platform Builder"
    - Solo team = add "Solo" prefix
    - Large team = add "Enterprise" prefix
    
    Return: { success: true, type: string, positionBoost: 5 }
  </action>
  <verify>
    - API accepts and saves role and disappointmentLevel
    - API returns personalized type
    - API returns position boost information
    - Validation rejects invalid values
  </verify>
  <done>
    Quiz API updated with new fields, position boost logic, and personalized type generation.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply migration and test complete flow</name>
  <files>
    drizzle/*.sql
  </files>
  <action>
    1. Apply the new migration to local D1:
       `npx wrangler d1 migrations apply illoominate-db --local`
    
    2. Build project to verify no TypeScript errors:
       `npm run build`
    
    3. Verify the complete flow works:
       - Quiz modal opens from waitlist success state
       - All 5 steps work correctly
       - API saves data correctly
       - Result screen displays
  </action>
  <verify>
    - Migration applied successfully
    - Build passes without errors
    - Quiz flow works end-to-end
  </verify>
  <done>
    Migration applied, build passes, quiz flow verified working.
  </done>
</task>

</tasks>

<verification>
Quiz redesign complete when:
1. Quiz shows one question per screen with progress bar
2. All 5 questions implemented with visual cards
3. PMF question captures disappointment level
4. Role field captured in first question
5. Personalized result screen shows type and position boost
6. Database schema updated with new fields
7. Build passes and flow works end-to-end
</verification>

<success_criteria>
- Quiz completion rate improved (one question per screen)
- PMF data captured (disappointment level)
- User role captured for segmentation
- Position boost incentive visible
- Personalized result increases engagement
- All existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page/01-05-SUMMARY.md`
</output>
