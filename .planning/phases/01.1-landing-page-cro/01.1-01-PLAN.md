---
phase: 01.1-landing-page-cro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hero.astro
  - src/components/TrustBlock.astro
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "Hero headline communicates outcome (what user achieves), not feature"
    - "CTA copy is specific and action-oriented ('Claim Early Access' not 'Sign Up')"
    - "Trust block appears immediately after hero section"
    - "Trust block shows framework platforms (iOS, Web) with momentum marker"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Outcome-oriented headline and optimized CTA"
      contains: "Ship fixes before"
    - path: "src/components/TrustBlock.astro"
      provides: "Social proof section with framework logos and waitlist count"
      min_lines: 40
    - path: "src/pages/index.astro"
      provides: "Page layout with TrustBlock after Hero"
      contains: "TrustBlock"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/TrustBlock.astro"
      via: "Astro component import"
      pattern: "import TrustBlock"
---

<objective>
Optimize above-the-fold messaging for conversion by implementing outcome-oriented hero copy and adding a trust block with social proof.

Purpose: Users decide within 10-20 seconds if they'll stay. Outcome-focused headlines convert 30%+ better than feature headlines. Trust signals above the fold build credibility faster.

Output:
- Updated Hero.astro with outcome-oriented headline and optimized CTA
- New TrustBlock.astro component with framework logos and momentum marker
- Updated index.astro integrating TrustBlock after Hero
</objective>

<execution_context>
@/Users/cipher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cipher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-landing-page-cro-conversion-optimization-for-waitlist-signups/01.1-RESEARCH.md
@src/components/Hero.astro
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hero with outcome-oriented messaging and CTA</name>
  <files>src/components/Hero.astro</files>
  <action>
Update the Hero.astro component with conversion-optimized copy:

1. **Headline** - Change from feature-focused to outcome-focused:
   - OLD: "Native feedback, not webviews"
   - NEW: "Ship fixes before users complain" with "Ship fixes before" on first line and "users complain" in indigo-400

2. **Tagline** - Keep the indie developer angle but add value clarity:
   - "Native feedback SDKs that feel like part of your app. Built for indie developers with pricing that scales with you, not against you."

3. **CTA Button** - Change from generic to specific:
   - OLD: "Join the Waitlist"
   - NEW: "Claim Early Access"

4. **Hint text** - Add founding member pricing angle:
   - "No credit card required. Founding member pricing for waitlist."

Keep all existing animations, background effects, and scroll indicator intact.
  </action>
  <verify>
    - `grep -q "Ship fixes before" src/components/Hero.astro` returns 0
    - `grep -q "Claim Early Access" src/components/Hero.astro` returns 0
    - `npm run build` succeeds
  </verify>
  <done>
    Hero displays outcome-oriented headline ("Ship fixes before users complain") and specific CTA ("Claim Early Access")
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TrustBlock component with social proof</name>
  <files>src/components/TrustBlock.astro</files>
  <action>
Create a new TrustBlock.astro component that displays immediately after the Hero section:

1. **Framework support section** - Show platforms Illoominate supports:
   - "Coming to" label
   - iOS icon with label (active, zinc-500)
   - Web icon with label (active, zinc-500)
   - Android icon with label (grayed out, opacity-50, "(soon)" badge)

2. **Momentum marker** - Dynamic waitlist count:
   - "Join X developers already on the waitlist"
   - X in indigo-400 font-semibold
   - For now, use a static placeholder like 142 (will be made dynamic later)

3. **Trust signals** - Key credibility markers:
   - Shield icon + "GDPR Compliant"
   - Lightning bolt icon + "Zero main thread impact"

Styling guidelines:
- Section: `py-12 border-y border-zinc-900`
- Container: `max-w-4xl mx-auto px-6`
- Use flex with flex-wrap for responsive layout
- Icons should be ~6x6 (w-6 h-6)
- Trust signals text: `text-xs text-zinc-600`
- Use emerald-500 for checkmark/trust icons

Reference the code example in 01.1-RESEARCH.md for SVG icons.
  </action>
  <verify>
    - File exists at `src/components/TrustBlock.astro`
    - `grep -q "GDPR Compliant" src/components/TrustBlock.astro` returns 0
    - `grep -q "Zero main thread impact" src/components/TrustBlock.astro` returns 0
  </verify>
  <done>
    TrustBlock component exists with framework logos (iOS, Web, Android coming soon), momentum marker, and trust signals
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate TrustBlock into landing page</name>
  <files>src/pages/index.astro</files>
  <action>
Update index.astro to include the TrustBlock component:

1. Add import for TrustBlock at the top with other component imports
2. Place `<TrustBlock />` immediately after `<Hero />` and before `<Features />`

The page structure should be:
```
<main>
  <Hero />
  <TrustBlock />
  <Features />
  <WaitlistForm />
  ...
</main>
```
  </action>
  <verify>
    - `grep -q "import TrustBlock" src/pages/index.astro` returns 0
    - `grep -q "<TrustBlock" src/pages/index.astro` returns 0
    - `npm run build` succeeds
    - `npm run preview` shows TrustBlock between Hero and Features (visual)
  </verify>
  <done>
    Landing page displays TrustBlock immediately after Hero section
  </done>
</task>

</tasks>

<verification>
Run full build and verify:
```bash
npm run build
```

Visual verification (manual):
1. Hero shows "Ship fixes before users complain" headline
2. Hero CTA reads "Claim Early Access"
3. Trust block visible immediately below hero
4. Trust block shows iOS, Web icons, Android (coming soon)
5. Momentum marker displays waitlist count
6. Trust signals (GDPR, zero main thread) are visible
</verification>

<success_criteria>
1. Hero section uses outcome-oriented messaging with "Ship fixes before users complain"
2. CTA copy is "Claim Early Access" (not generic "Join Waitlist")
3. Trust block displays framework logos (iOS, Web, Android coming soon)
4. Trust block shows momentum marker with waitlist count
5. Trust block shows trust signals (GDPR Compliant, Zero main thread impact)
6. Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-landing-page-cro/01.1-01-SUMMARY.md`
</output>
