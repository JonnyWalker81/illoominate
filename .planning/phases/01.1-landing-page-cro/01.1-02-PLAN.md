---
phase: 01.1-landing-page-cro
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WaitlistForm.astro
  - src/components/QuizModal.astro
  - src/components/StickyCTA.astro
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "Quiz CTA shows +5 position boost as primary incentive"
    - "Quiz framing emphasizes time (30 sec) and reward (jump ahead)"
    - "Mobile users see sticky CTA after scrolling past hero"
    - "Sticky CTA disappears when waitlist section is in view"
  artifacts:
    - path: "src/components/WaitlistForm.astro"
      provides: "Updated quiz incentive card with position boost framing"
      contains: "+5"
    - path: "src/components/StickyCTA.astro"
      provides: "Mobile-only sticky CTA with IntersectionObserver logic"
      min_lines: 30
    - path: "src/pages/index.astro"
      provides: "Page layout with StickyCTA component"
      contains: "StickyCTA"
  key_links:
    - from: "src/components/StickyCTA.astro"
      to: "#waitlist"
      via: "Anchor link"
      pattern: 'href="#waitlist"'
    - from: "src/pages/index.astro"
      to: "src/components/StickyCTA.astro"
      via: "Astro component import"
      pattern: "import StickyCTA"
---

<objective>
Optimize post-signup engagement and mobile conversion by implementing gamified quiz framing and a sticky mobile CTA.

Purpose: Gamification drives quiz completion. Sticky mobile CTAs increase conversion by keeping the action accessible (252% lift in case studies). Mobile users need persistent access to action as they scroll.

Output:
- Updated WaitlistForm.astro with enhanced quiz incentive framing
- New StickyCTA.astro component for mobile with IntersectionObserver
- Updated index.astro integrating StickyCTA
</objective>

<execution_context>
@/Users/cipher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cipher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-landing-page-cro-conversion-optimization-for-waitlist-signups/01.1-RESEARCH.md
@src/components/WaitlistForm.astro
@src/components/QuizModal.astro
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update quiz incentive framing in WaitlistForm</name>
  <files>src/components/WaitlistForm.astro</files>
  <action>
Update the quiz CTA card in the success state of WaitlistForm.astro:

1. **Change the visual design** to emphasize the position boost:
   - Update gradient from indigo/purple to emerald/indigo: `from-emerald-500/10 to-indigo-500/10`
   - Update border to emerald: `border-emerald-500/30`

2. **Update the incentive icon** to show +5 prominently:
   - Replace the gift icon div with a text-based position badge
   - Use `bg-emerald-500/20` background
   - Display "+5" in `text-emerald-400 font-bold text-lg`

3. **Update the copy**:
   - Primary: "Jump ahead in line" (not "Move up 5 spots!")
   - Secondary: "Answer 3 quick questions (30 sec)" (not "Take our 30-second quiz")

4. **Update button**:
   - Change from indigo to emerald: `bg-emerald-600 hover:bg-emerald-500`
   - Change text from "Start Quiz" to just "Start"

Keep all existing JavaScript functionality intact.
  </action>
  <verify>
    - `grep -q "Jump ahead in line" src/components/WaitlistForm.astro` returns 0
    - `grep -q '"+5"' src/components/WaitlistForm.astro` or `grep -q "+5" src/components/WaitlistForm.astro` returns 0
    - `grep -q "emerald-600" src/components/WaitlistForm.astro` returns 0
    - `npm run build` succeeds
  </verify>
  <done>
    Quiz CTA card shows "+5" badge prominently, uses "Jump ahead in line" copy, and emphasizes 30-second time commitment
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sticky mobile CTA component</name>
  <files>src/components/StickyCTA.astro</files>
  <action>
Create a new StickyCTA.astro component for mobile devices:

1. **Container styling**:
   - Fixed position at bottom: `fixed bottom-0 left-0 right-0`
   - Z-index below modals: `z-40`
   - Hidden on desktop: `md:hidden`
   - Padding with safe area: `p-4 pb-[calc(1rem+env(safe-area-inset-bottom))]`
   - Background: `bg-[#0a0a0a]/95 backdrop-blur-sm`
   - Top border: `border-t border-zinc-800`
   - Start hidden (translated down): `transform translate-y-full`
   - Transition: `transition-transform duration-300`

2. **CTA button**:
   - Full width block button
   - Text: "Claim Early Access" (matches hero CTA)
   - Link to: `#waitlist`
   - Styling: `bg-indigo-600 hover:bg-indigo-500 text-white text-center font-semibold rounded-xl py-3.5`

3. **JavaScript for visibility**:
   - Use IntersectionObserver to detect when hero scrolls out of view
   - Show sticky CTA (remove `translate-y-full`) when hero is not visible
   - Hide sticky CTA (add `translate-y-full`) when hero IS visible
   - Also hide when waitlist section is in view (user is at the form already)
   - Threshold: 0.1 for hero, 0.3 for waitlist

Reference the code example in 01.1-RESEARCH.md for the IntersectionObserver pattern.

**Important:** Give the sticky CTA div an ID like `sticky-cta` for the observer to target.
  </action>
  <verify>
    - File exists at `src/components/StickyCTA.astro`
    - `grep -q "md:hidden" src/components/StickyCTA.astro` returns 0
    - `grep -q "IntersectionObserver" src/components/StickyCTA.astro` returns 0
    - `grep -q "#waitlist" src/components/StickyCTA.astro` returns 0
  </verify>
  <done>
    StickyCTA component exists with mobile-only visibility, IntersectionObserver-based show/hide, and "Claim Early Access" button
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate StickyCTA into landing page</name>
  <files>src/pages/index.astro</files>
  <action>
Update index.astro to include the StickyCTA component:

1. Add import for StickyCTA at the top with other component imports

2. Place `<StickyCTA />` AFTER the closing `</main>` tag but BEFORE `<QuizModal />`
   - This ensures it's outside main content flow
   - Z-index 40 keeps it below QuizModal (which should be z-50)

3. Add bottom padding to main content to prevent sticky CTA from covering content on mobile:
   - Add `class="pb-20 md:pb-0"` to the `<main>` element
   - This adds padding on mobile only, where sticky CTA appears

The page structure should be:
```astro
<Layout>
  <main class="pb-20 md:pb-0">
    <Hero />
    <TrustBlock />
    <Features />
    <WaitlistForm />
    <footer>...</footer>
  </main>

  <StickyCTA />
  <QuizModal />
</Layout>
```
  </action>
  <verify>
    - `grep -q "import StickyCTA" src/pages/index.astro` returns 0
    - `grep -q "<StickyCTA" src/pages/index.astro` returns 0
    - `grep -q "pb-20 md:pb-0" src/pages/index.astro` returns 0
    - `npm run build` succeeds
  </verify>
  <done>
    Landing page includes StickyCTA component, positioned correctly with bottom padding for mobile
  </done>
</task>

</tasks>

<verification>
Run full build and verify:
```bash
npm run build
```

Visual verification (manual):
1. On mobile viewport (or dev tools mobile simulation):
   - Scroll past hero section
   - Sticky CTA appears at bottom
   - Scroll to waitlist section
   - Sticky CTA hides
2. Quiz CTA in success state shows:
   - "+5" badge prominently displayed
   - "Jump ahead in line" text
   - "Answer 3 quick questions (30 sec)" subtext
   - Green/emerald "Start" button
3. On desktop, sticky CTA should NOT appear at any scroll position
</verification>

<success_criteria>
1. Quiz CTA card shows "+5" position boost prominently
2. Quiz framing uses "Jump ahead in line" and "30 sec" time estimate
3. Sticky CTA appears on mobile after scrolling past hero
4. Sticky CTA hides when waitlist section is visible
5. Sticky CTA is hidden on desktop (md:hidden)
6. Bottom padding prevents content overlap on mobile
7. Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-landing-page-cro/01.1-02-SUMMARY.md`
</output>
