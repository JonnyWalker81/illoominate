---
phase: 02-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260122000000_initial_extensions.sql
  - .env.example
  - package.json
autonomous: true
user_setup:
  - service: supabase
    why: "Database and auth provider"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (keep secret)"
    dashboard_config:
      - task: "Create Supabase project"
        location: "https://supabase.com/dashboard -> New project"

must_haves:
  truths:
    - "Supabase CLI is initialized in project"
    - "Local Supabase stack can start"
    - "Environment variables documented"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Supabase local configuration"
    - path: "supabase/migrations/20260122000000_initial_extensions.sql"
      provides: "Required PostgreSQL extensions"
    - path: ".env.example"
      provides: "Environment variable template"
  key_links:
    - from: "supabase/config.toml"
      to: "local Supabase stack"
      via: "supabase start command"
      pattern: "project_id"
---

<objective>
Initialize Supabase project structure with CLI and configure for local development.

Purpose: Establish the foundation for database migrations and local development workflow
Output: Supabase CLI initialized, local stack configuration ready
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Supabase CLI and project structure</name>
  <files>supabase/config.toml, package.json</files>
  <action>
    1. Check if Supabase CLI is available, if not provide installation instructions
    2. Run `supabase init` to create the supabase/ directory structure
    3. Update config.toml with project settings:
       - project_id = "illoominate"
       - Set local ports that don't conflict with existing landing page (5432 for DB, 54321 for API, 54323 for Studio)
    4. Add @supabase/supabase-js to package.json dependencies
    5. Create .env.example with placeholder Supabase variables:
       - SUPABASE_URL
       - SUPABASE_ANON_KEY
       - SUPABASE_SERVICE_ROLE_KEY
       - DATABASE_URL (for direct Postgres access if needed)
  </action>
  <verify>
    - supabase/config.toml exists and contains project_id = "illoominate"
    - supabase/migrations/ directory exists
    - supabase/seed.sql exists (even if empty)
    - .env.example contains all Supabase variables
  </verify>
  <done>Supabase project structure initialized with proper configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create initial extensions migration</name>
  <files>supabase/migrations/20260122000000_initial_extensions.sql</files>
  <action>
    Create the first migration file that enables required PostgreSQL extensions:
    
    ```sql
    -- Enable UUID generation
    create extension if not exists "uuid-ossp";
    
    -- Enable cryptographic functions (for API key hashing)
    create extension if not exists "pgcrypto";
    ```
    
    These extensions are used by all subsequent schema migrations.
    The uuid-ossp extension provides gen_random_uuid() for primary keys.
    The pgcrypto extension provides crypt() for secure API key comparison.
  </action>
  <verify>
    - Migration file exists at supabase/migrations/20260122000000_initial_extensions.sql
    - File contains CREATE EXTENSION statements for uuid-ossp and pgcrypto
  </verify>
  <done>Initial extensions migration ready for application</done>
</task>

</tasks>

<verification>
1. `ls supabase/` shows config.toml, migrations/, seed.sql
2. `cat supabase/config.toml | grep project_id` returns "illoominate"
3. `ls supabase/migrations/` shows the initial extensions migration
4. `cat .env.example | grep SUPABASE` shows all required variables
</verification>

<success_criteria>
- [ ] supabase/ directory structure created
- [ ] config.toml configured with illoominate project settings
- [ ] Initial migration file created with extensions
- [ ] .env.example documents required environment variables
- [ ] @supabase/supabase-js added to dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundation/02-01-SUMMARY.md`
</output>
