---
const steps = [
  {
    number: '01',
    title: 'Collect Feedback',
    description: 'Embed our widget, connect integrations, or use the API. All feedback lands in your unified inbox automatically.',
    visual: 'inbox',
  },
  {
    number: '02',
    title: 'Organize & Prioritize',
    description: 'AI categorizes and detects duplicates. Users vote on what matters. You see what to build next.',
    visual: 'organize',
  },
  {
    number: '03',
    title: 'Share Your Roadmap',
    description: 'Publish a beautiful public roadmap. Show users you\'re listening and keep them engaged.',
    visual: 'roadmap',
  },
  {
    number: '04',
    title: 'Close the Loop',
    description: 'Ship features and automatically notify everyone who asked. Turn feedback into loyalty.',
    visual: 'notify',
  },
];
---

<section id="how-it-works" class="section-padding bg-slate-900">
  <div class="max-w-7xl mx-auto">
    <div class="text-center mb-16">
      <span class="inline-block px-4 py-1 bg-emerald-900/50 text-emerald-300 rounded-full text-sm font-medium mb-4">How It Works</span>
      <h2 class="section-title">From Chaos to Clarity in 4 Steps</h2>
      <p class="section-subtitle">
        Transform scattered feedback into a data-driven product strategy.
      </p>
    </div>

    <div class="relative">
      <!-- Connection line (hidden on mobile) -->
      <div class="hidden lg:block absolute top-24 left-[calc(12.5%-16px)] right-[calc(12.5%-16px)] h-0.5 bg-gradient-to-r from-indigo-700 via-purple-700 to-emerald-700"></div>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {steps.map((step, index) => (
          <div class="relative">
            <!-- Step number badge -->
            <div class="flex items-center justify-center lg:justify-start mb-6">
              <div class="relative">
                <div class="w-12 h-12 rounded-full bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center text-white font-bold text-lg shadow-lg">
                  {step.number}
                </div>
                <!-- Connector dot -->
                <div class="hidden lg:block absolute top-1/2 -right-2 transform translate-x-full -translate-y-1/2 w-4 h-4 rounded-full bg-slate-800 border-2 border-indigo-500"></div>
              </div>
            </div>

            <div class="text-center lg:text-left">
              <h3 class="text-xl font-semibold text-white mb-3">{step.title}</h3>
              <p class="text-slate-400">{step.description}</p>
            </div>

            <!-- Visual indicator based on step -->
            {step.visual === 'inbox' && (
              <div class="mt-6 p-4 bg-slate-800 rounded-xl shadow-sm border border-slate-700">
                <div class="space-y-2">
                  <div class="flex items-center gap-2 p-2 bg-slate-700/50 rounded-lg">
                    <div class="w-2 h-2 rounded-full bg-indigo-500"></div>
                    <div class="flex-1 h-2 bg-slate-600 rounded"></div>
                  </div>
                  <div class="flex items-center gap-2 p-2 bg-slate-700/50 rounded-lg">
                    <div class="w-2 h-2 rounded-full bg-purple-500"></div>
                    <div class="flex-1 h-2 bg-slate-600 rounded"></div>
                  </div>
                  <div class="flex items-center gap-2 p-2 bg-slate-700/50 rounded-lg">
                    <div class="w-2 h-2 rounded-full bg-emerald-500"></div>
                    <div class="flex-1 h-2 bg-slate-600 rounded"></div>
                  </div>
                </div>
              </div>
            )}

            {step.visual === 'organize' && (
              <div class="mt-6 p-4 bg-slate-800 rounded-xl shadow-sm border border-slate-700">
                <div class="flex items-center justify-between mb-3">
                  <span class="text-xs font-medium text-slate-400">Priority Score</span>
                  <span class="text-xs font-bold text-indigo-400">87%</span>
                </div>
                <div class="w-full h-2 bg-slate-600 rounded-full overflow-hidden">
                  <div class="h-full bg-gradient-to-r from-indigo-500 to-purple-500 rounded-full" style="width: 87%"></div>
                </div>
                <div class="flex gap-2 mt-3">
                  <span class="px-2 py-0.5 bg-indigo-900/50 text-indigo-300 text-xs rounded">Feature</span>
                  <span class="px-2 py-0.5 bg-emerald-900/50 text-emerald-300 text-xs rounded">High Impact</span>
                </div>
              </div>
            )}

            {step.visual === 'roadmap' && (
              <div class="mt-6 p-4 bg-slate-800 rounded-xl shadow-sm border border-slate-700">
                <div class="space-y-3">
                  <div class="flex items-center gap-2">
                    <div class="w-3 h-3 rounded-full bg-emerald-500"></div>
                    <span class="text-xs font-medium text-slate-300">Now</span>
                    <div class="flex-1 h-0.5 bg-emerald-700"></div>
                  </div>
                  <div class="flex items-center gap-2">
                    <div class="w-3 h-3 rounded-full bg-amber-500"></div>
                    <span class="text-xs font-medium text-slate-300">Next</span>
                    <div class="flex-1 h-0.5 bg-amber-700"></div>
                  </div>
                  <div class="flex items-center gap-2">
                    <div class="w-3 h-3 rounded-full bg-slate-500"></div>
                    <span class="text-xs font-medium text-slate-300">Later</span>
                    <div class="flex-1 h-0.5 bg-slate-600"></div>
                  </div>
                </div>
              </div>
            )}

            {step.visual === 'notify' && (
              <div class="mt-6 p-4 bg-slate-800 rounded-xl shadow-sm border border-slate-700">
                <div class="flex items-start gap-3">
                  <div class="w-8 h-8 rounded-full bg-emerald-900/50 flex items-center justify-center flex-shrink-0">
                    <svg class="w-4 h-4 text-emerald-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                    </svg>
                  </div>
                  <div>
                    <p class="text-xs font-medium text-white">Feature Shipped!</p>
                    <p class="text-xs text-slate-400">12 users notified</p>
                  </div>
                </div>
              </div>
            )}
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
