# Use the Nix flake dev environment
use flake

# Load local environment variables if .env exists
dotenv_if_exists .env

# Watch additional files for changes
watch_file flake.nix
watch_file flake.lock
